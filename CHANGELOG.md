# 更新记录

## 0.11.0 - 2026-08-14

- 新增启动后台检查和顶部“检查更新”窗口，展示当前版本、最新版本、Release 说明、更新模式和下载大小。
- 新增无 REST 配额的 GitHub Release 通道清单，并在清单缺失、API 限流时降级到 latest 重定向检测。
- 优先匹配当前版本到最新版本的增量 ZIP；基线不匹配或增量资产缺失时自动回退完整安装程序。
- 新增资产大小、外层 SHA-256、增量清单和逐文件 SHA-256 多层校验，拒绝非 GitHub URL和不安全 ZIP 路径。
- 新增独立单文件更新器：退出桌面和本地服务后更新 Program Files，按文件备份，失败恢复并重新启动应用。
- 新增增量包、便携基线、更新通道和 GitHub Release 工作流；自动化测试覆盖增量选择、完整回退、通道清单和文件应用。

## 0.10.0 - 2026-08-14

- 新增“安全修改”模块，把模型优化候选先隐藏到受控 PBIP/TMDL 隔离副本，源项目永不写入。
- 新增计划预览、候选状态复核、确认短语和最大操作数门禁；被引用对象、实时 PBIX 和 model.bim 自动阻断。
- 新增项目内容 SHA-256 指纹，计划生成后的任何源文件变化都会阻止应用。
- 新增逐文件备份、同目录原子写入、持久化 JSON 审计、每计划并发锁和副本回滚。
- 新增安全修改 API、桌面界面及源文件不变、阻断候选、漂移检测、应用/回滚自动化测试。

## 0.9.1 - 2026-08-14

- 新增 PowerTools 品牌应用图标及 16–256px 多尺寸 Windows ICO。
- 桌面窗口、任务栏、EXE、Setup、开始菜单和桌面快捷方式统一使用应用图标。
- Power BI 外部工具清单改用真实 64px PNG 图标，移除 1×1 占位图。
- 新增可复现的图标转换脚本和 Power BI Base64 图标资源。

## 0.9.0 - 2026-08-14

- 新增标准 Windows Setup EXE，默认安装到 `C:\Program Files\PowerTools`。
- 安装阶段自动注册 Power BI Desktop 外部工具，无需再手动运行注册脚本。
- 卸载阶段自动删除 PowerTools 外部工具清单、快捷方式、App Paths 和程序文件。
- 安装包默认包含 .NET 8 Desktop 与 ASP.NET Core 运行时，目标电脑无需单独安装 .NET。
- 新增开始菜单入口、可选桌面快捷方式和“应用和功能”卸载入口。
- 新增安装器自动构建及完整安装、健康检查、注册、卸载生命周期测试脚本。
- 发布清单排除调试符号、本机测试数据和无关语言资源。

## 0.8.0 - 2026-08-14

- 新增“报表质量”中心，提供问题概览、标题/替代文本覆盖率、页面与类别筛选和全文搜索。
- 新增标题、替代文本、Tab 顺序、视觉密度、网格对齐、视觉筛选器密度、Tooltip 页面和钻取返回入口等 PBIR 规则。
- 解析报表级、页面级和视觉级筛选器以及 Tooltip、钻取、标题、替代文本和交互属性。
- 质量问题支持点击后切换到对应页面，并在布局画布中选中具体视觉对象。
- 布局检查识别书签管理的视觉层，减少动态切换视觉造成的重叠与重复误报。
- Power Query 新增 `report-quality` 实体，并扩充 `pages`、`visuals`、`issues` 和 `summary` 字段。
- 版本比较纳入报表筛选器以及页面质量相关属性。

## 0.7.0 - 2026-08-13

- 新增 Power BI Desktop 外部工具模式，通过 `%server%` / `%database%` 和 TOM 读取当前打开模型。
- 新增 VertiPaq DMV 存储指标与“模型优化”存储视图。
- 删除候选纳入 SortByColumn、键字段、实时模型证据和估算回收空间。
- 新增 Power BI 外部工具注册/卸载脚本，并限制实时连接只能指向本机。
- Power Query 新增 `storage-metrics` 实体并扩充字段/删除候选列。
- 根据真实 Power BI Desktop DMV 修正物理层次结构重复行，并通过 TMSCHEMA 统计映射准确的字段行数与基数。
- 桌面发布包新增可自动提权并识别同目录 EXE 的一键外部工具注册入口。

## 0.6.0 - 2026-08-13

- 新增 WPF + WebView2 桌面入口，无黑色控制台并支持单实例运行。
- 桌面窗口启动随机回环端口服务，关闭窗口时同步退出后端。
- 新增 `live`、`ready` 和诊断接口。
- 新增 `/api/v1` 版本化 API，并保留旧接口兼容。
- 新增响应压缩、请求限流和 JSON 结构化日志。
- 新增项目文件稳定检测和解析失败时的上一次成功快照回退。
- 新增桌面发布脚本和运行说明。

## 0.5.0 - 2026-08-13

- 新增模型优化模块。
- 新增字段与度量值删除候选、阻断条件、风险分级和引用证据。
- 新增 9 类度量值静态优化规则，覆盖 Microsoft 最佳实践与 SQLBI 公开优化方法。
- 新增 Power Query 删除候选与度量优化实体。

## 0.4.0 - 2026-08-13

- 新增项目版本比较 API 和管理界面。
- 缓存支持首次并发请求合并和项目文件变更自动失效。
- 新增可配置项目目录白名单。
- 降低装饰对象、隐藏书签层和完全堆叠对象造成的布局重叠误报。
- 新增 xUnit 自动化测试和 GitHub Actions CI。

## 0.3.0 - 2026-08-13

- 新增 Power Query 扁平数据接口和实体目录。
- 新增项目快照缓存，避免多张查询重复扫描项目。
- 新增通用 Power Query M 函数和 Power BI Service 网关说明。

## 0.2.0 - 2026-08-13

- 重构模型导航，将度量值、计算组和依赖统一收纳到模型模块。
- 完善 TMDL 度量值、计算组、RLS 和 OLS 解析。
- 新增度量值、字段和计算项的交互式上下游依赖图。
- 新增 PBIR 页面布局画布。
- 新增 PBIR 书签、书签组、筛选器和视觉对象显隐状态识别。
- 新增书签与页面布局联动预览及孤立书签检查。
- 增加非标准书签 JSON 的容错解析。
- 补充使用指南、架构说明和构建发布文档。

## 0.1.0

- 初始本地 Power BI 项目解析和管理界面。
